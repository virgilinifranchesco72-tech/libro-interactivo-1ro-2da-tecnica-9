/* Lector limpio: un estado, una hoja y una animación controlada. */
const paper=document.querySelector('#paper'),label=document.querySelector('#label'),seasonTag=document.querySelector('#seasonTag'),pageCount=document.querySelector('#pageCount'),progress=document.querySelector('#progress');let position=0,soundOn=false;const total=CHAPTERS.length*2+2;
const esc=v=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function put(html,kind=''){paper.className='paper '+kind;paper.innerHTML=html}
function render(){if(position===0){put('<p class="season">TÉCNICA 9 · 1RO 2DA</p><h1>EL AULA QUE<br>APRENDIÓ A ESCUCHARSE</h1><p>Dos temporadas. Cien capítulos. Una historia completamente nueva.</p><p><b>Escritor: primero segunda</b></p><p>“Nadie aprende solo porque…”</p>','cover');label.textContent='Portada';seasonTag.textContent='NUEVA HISTORIA'}else if(position===total-1){put('<p class="season">CONTRATAPA</p><h2>La última página también enseña</h2><p>“…nadie llega al final sin que alguien le enseñe una parte del camino.”</p><p>Fin.</p>','cover');label.textContent='Contratapa';seasonTag.textContent='FIN'}else{const i=Math.floor((position-1)/2),part=(position-1)%2+1,c=CHAPTERS[i];const extra=part===2?`<p>${esc(c.continuation||'El curso encontró una nueva pista y avanzó unido.')}</p>`:'';put(`<p class="season">${esc(c.season)}</p><h2>Capítulo ${c.num}<br>${esc(c.title)}</h2><p><b>Docente:</b> ${esc(c.teacher)}</p><p>${esc(c.text)}</p>${extra}<span class="number">Página ${part} de 2</span>`);label.textContent=`Capítulo ${c.num} · página ${part} de 2`;seasonTag.textContent=c.season.replace('Temporada ','T')};pageCount.textContent=`${position} / ${total-1}`;progress.style.width=`${position/(total-1)*100}%`;document.querySelector('#prev').disabled=position===0;document.querySelector('#next').disabled=position===total-1}
function move(step){const target=position+step;if(target<0||target>=total)return;paper.classList.add('turn');setTimeout(()=>{position=target;render();paper.classList.remove('turn')},350);if(soundOn&&'speechSynthesis'in window){const u=new SpeechSynthesisUtterance(step>0?'Pasando página':'Página anterior');u.lang='es-AR';speechSynthesis.cancel();speechSynthesis.speak(u)}}
document.querySelector('#next').onclick=()=>move(1);document.querySelector('#prev').onclick=()=>move(-1);document.querySelector('#sound').onclick=()=>{soundOn=!soundOn;document.querySelector('#sound').textContent=soundOn?'◉ Sonido activo':'◌ Sonido'};document.querySelector('#full').onclick=()=>document.documentElement.requestFullscreen?.();document.addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key===' ')move(1);if(e.key==='ArrowLeft')move(-1)});render();